# Runtime: paper XGBoost scorer only
numpy>=1.24
xgboost>=2.0

# Optional — LLM feature extraction (pick one provider)
# openai>=1.30
# anthropic>=0.30
# google-genai>=1.0
# pyyaml>=6.0

# Optional — rebuild encoder_state from StoryScope parquet
# pyarrow>=14.0
