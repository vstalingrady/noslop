# noslop v2 Implementation Plan

> **For agentic workers:** Use subagent-driven-development or executing-plans. Checkbox steps.

**Goal:** Redesign noslop so ship quality is **reader voice / anti-slop** (primary), with optional detector probe and StoryScope demoted to diagnostic — fix GPTZero-and-books failure of v1.

**Architecture:** VOICE grade + surface bans; drop forced StoryScope must-hit stack; new PRE-WRITE; voice CLI helper; A/B eval on VOICE; human baseline docs stay as warning.

**Tech Stack:** Markdown skill, optional Python `noslop voice` heuristics, existing StoryScope CLI untouched except docs.

**Spec:** `docs/superpowers/specs/2026-07-15-noslop-v2-redesign.md`

---

## File map

| Path | Action |
|------|--------|
| `skills/noslop/SKILL.md` | Rewrite v2 iron law + loop |
| `skills/noslop/checklists.md` | PRE-WRITE v2 + VOICE grade |
| `skills/noslop/human_coding.md` | Demote to “optional StoryScope mode” |
| `skills/noslop/voice.md` | **Create** — VOICE axes + anti-templates |
| `skills/noslop/style-and-bans.md` | Keep; call after VOICE |
| `skills/noslop/core_features.md` | Mark StoryScope optional / diagnostic |
| `src/noslop/voice.py` | **Create** — heuristic VOICE score from text |
| `src/noslop/cli.py` | Add `voice` subcommand |
| `tests/test_voice.py` | TDD for moral_close, bans, anchors |
| `evals/run_voice_ab.py` | A/B default vs noslop-v2 on VOICE |
| `evals/results/SUMMARY_V2.md` | Numbers |
| `README.md` | Dual-goal docs |
| Mirror skill → `~/.claude/skills/noslop/` | Install |

---

### Task 1: VOICE heuristics CLI (TDD)

**Files:** `src/noslop/voice.py`, `src/noslop/cli.py`, `tests/test_voice.py`

- [ ] **Step 1:** Failing tests for:
  - moral-close patterns → hard fail or low score
  - ban hits count
  - anchor density (proper noun / digit / time-ish)
  - sentence length variance
  - PASS threshold 6.5

- [ ] **Step 2:** Implement `score_voice(text: str) -> dict` with keys: `score`, `gate`, `hard_fail`, `axes`, `details`.

- [ ] **Step 3:** `python -m noslop.cli voice --text-file draft.md --json`

- [ ] **Step 4:** pytest green; commit.

---

### Task 2: Rewrite skill pack

**Files:** `skills/noslop/*`

- [ ] **Step 1:** Write `voice.md` (axes, anti-templates, genre notes).

- [ ] **Step 2:** Rewrite `SKILL.md`:
  - Primary ship = VOICE MERGED PASS
  - StoryScope optional (`mode: storyscope` only if user asks)
  - Drop forced aftermath/twist/frame every draft
  - Add skip / boring detail / no thesis close

- [ ] **Step 3:** Update `checklists.md`, demote `human_coding.md` + `core_features.md`.

- [ ] **Step 4:** Mirror to `%USERPROFILE%\.claude\skills\noslop\`.

- [ ] **Step 5:** Commit.

---

### Task 3: A/B eval on VOICE

**Files:** `evals/run_voice_ab.py`, drafts under `evals/results/v2/`

- [ ] **Step 1:** For each of 5 briefs: write **default** and **noslop-v2** drafts (agent follows new skill).

- [ ] **Step 2:** Run `noslop voice` on each; fill SUMMARY_V2.md.

- [ ] **Step 3:** Pass if noslop VOICE mean ≥ 6.5 and ≥ default+1.5 on ≥4/5.

- [ ] **Step 4:** Optional: manual GPTZero on 1–2 samples; record AI% in SUMMARY (no fail if tool missing).

- [ ] **Step 5:** Commit.

---

### Task 4: Docs + anti-confusion

**Files:** `README.md`, link `HUMAN_BASELINE.md`

- [ ] State clearly: StoryScope ≠ books ≠ GPTZero.
- [ ] How to run voice / optional storyscope.
- [ ] Commit.

---

### Task 5: Sample ship

- [ ] Produce one flagship sample under `evals/results/v2/sample_flagship.md`.
- [ ] VOICE PASS vs default of same brief.
- [ ] If GPTZero available, screenshot/result path noted in SUMMARY_V2.

---

## Self-review

| Spec criterion | Task |
|----------------|------|
| VOICE primary | 1, 2, 3 |
| Detector optional | 3, 5 |
| StoryScope demoted | 2, 4 |
| Human baseline honesty | 4 |
| Skill mirror | 2 |
| Sample | 5 |

---

## Execution handoff

Plan: `docs/superpowers/plans/2026-07-15-noslop-v2.md`  
Spec: `docs/superpowers/specs/2026-07-15-noslop-v2-redesign.md`

Then paste **GOAL PROMPT** below into `/goal`.
